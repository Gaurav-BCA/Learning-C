#include<stdio.h>

int main()
{
    int marks[90]; //Reverse space to store 90 integers
    marks[0] = 98;
    marks[1] = 88;
    // We can go all the way till marks[89]
    printf("Marks of 0 is %d and Marks of 1 is %d\n", marks[0], marks[1]);
    return 0;
}