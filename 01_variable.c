#include <stdio.h>

int main()
{
    int a, b; // Variable declaration
    a = 8;    // Variable intialization
    b = 9;    // Variable intialization
    int sum = a + b;
    printf("the output of this program is %d", sum);
    return 0;
}
