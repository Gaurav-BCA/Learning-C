#include <stdio.h>

int main()
{
    int age = 5;
    if (age > 10)
    {
        printf("we are inside if\n");
        printf("your age is greater than 10\n");
    }
    else
    {
        printf("we are inside else\n");
        printf("ypur age is not greater than 10");
    }
    return 0;
}